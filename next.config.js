/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'upload.wikimedia.org', 'cdn.sanity.io']
    }
}

module.exports = nextConfig
